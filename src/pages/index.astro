---
import BaseLayout from "../layouts/BaseLayout.astro";
import { getCollection } from "astro:content";
import { PUBLICATIONS } from "../consts";

// Get recent posts (latest 5)
const posts = (await getCollection("posts"))
  .filter((post) => !post.data.draft)
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf())
  .slice(0, 5);

// Get research papers
const research = (await getCollection("research"))
  .filter((paper) => !paper.data.draft)
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf())
  .slice(0, 3);

// Better date formatting - full date with day
function formatDate(date: Date): string {
  return date.toLocaleDateString("en-US", {
    year: "numeric",
    month: "long",
    day: "numeric",
  });
}

// Research interests
const researchInterests = [
  {
    area: "Programming Languages",
    description: "Type systems, operational semantics, and language design",
  },
  {
    area: "Distributed Systems",
    description:
      "Consistency models, fault tolerance, and distributed algorithms",
  },
  {
    area: "Developer Tools",
    description: "Static analysis, build systems, and developer experience",
  },
];
---

<BaseLayout>
  <div class="container">
    <!-- Hero Section with Photo -->
    <section class="hero">
      <div class="hero-intro">
        <img
          src="https://avatars.githubusercontent.com/u/17507528?v=4"
          alt="Nana Adjei Manu"
          class="profile-photo"
        />
        <div class="hero-content">
          <p class="hero-subtitle">
            I'm a software engineer and researcher interested in programming
            languages, distributed systems, and developer tools. I'm currently
            exploring how to make software systems more reliable and easier to
            reason about.
          </p>
          <div class="hero-links">
            <a href="mailto:n.k.a.manu06@gmail.com" class="hero-link">Email</a>
            <a
              href="https://github.com/naamanu"
              class="hero-link"
              target="_blank"
              rel="noopener">GitHub</a
            >
            <a
              href="https://linkedin.com/in/nana-adjei-manu"
              class="hero-link"
              target="_blank"
              rel="noopener">LinkedIn</a
            >
            <a
              href="https://twitter.com/nmanu__"
              class="hero-link"
              target="_blank"
              rel="noopener">Twitter</a
            >
          </div>
        </div>
      </div>
    </section>

    <!-- Research Interests -->
    <section class="section">
      <div class="section-header">
        <h2 class="section-title">Research Interests</h2>
      </div>
      <div class="interests-list">
        {
          researchInterests.map((interest) => (
            <div class="interest-item">
              <span class="interest-area">{interest.area}</span>
              <span class="interest-desc">{interest.description}</span>
            </div>
          ))
        }
      </div>
    </section>

    <!-- Research Section -->
    {
      research.length > 0 && (
        <section class="section">
          <div class="section-header">
            <h2 class="section-title">Selected Publications</h2>
            <a href="/research" class="section-link">
              All papers →
            </a>
          </div>
          <div class="publications-list">
            {research.map((paper) => (
              <div class="publication-item">
                <a href={`/research/${paper.slug}/`} class="publication-title">
                  {paper.data.title}
                </a>
                {paper.data.venue && (
                  <p class="publication-venue">{paper.data.venue}</p>
                )}
              </div>
            ))}
          </div>
        </section>
      )
    }

    <!-- Writing Section -->
    <section class="section">
      <div class="section-header">
        <h2 class="section-title">Writing</h2>
        <a href="/posts" class="section-link">All posts →</a>
      </div>
      <div class="post-list">
        {
          posts.map((post) => (
            <article class="post-item">
              <a href={`/posts/${post.slug}/`} class="post-link">
                {post.data.title}
              </a>
              <time class="post-date" datetime={post.data.date.toISOString()}>
                {formatDate(post.data.date)}
              </time>
            </article>
          ))
        }
      </div>
    </section>

    <!-- Background Section -->
    <section class="section">
      <div class="section-header">
        <h2 class="section-title">Background</h2>
      </div>
      <p>
        I've worked as a software engineer at companies including
        <a href="https://rhino.com">Rhino</a>,
        <a href="https://leafly.com">Leafly</a>,
        <a href="https://andela.com">Andela</a>, and
        <a href="https://housinganywhere.com">HousingAnywhere</a>. My work has
        spanned frontend, backend, and infrastructure.
      </p>
      <p>
        I hold a Master's degree in Advanced Computer Science from the
        University of Leicester (Distinction). Outside of work, I enjoy
        functional programming, reading, and playing sports.
      </p>
    </section>
  </div>
</BaseLayout>

<style>
  .hero-intro {
    display: flex;
    gap: var(--space-lg);
    align-items: flex-start;
  }

  .profile-photo {
    width: 90px;
    height: 90px;
    border-radius: 50%;
    object-fit: cover;
    flex-shrink: 0;
    border: 3px solid var(--color-border);
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
    filter: grayscale(10%);
    transition: filter 0.2s ease;
  }

  .profile-photo:hover {
    filter: grayscale(0%);
  }

  .hero-content {
    flex: 1;
  }

  .interests-list {
    display: flex;
    flex-direction: column;
    gap: var(--space-sm);
  }

  .interest-item {
    display: flex;
    gap: var(--space-md);
  }

  .interest-area {
    font-weight: 500;
    color: var(--color-text);
    min-width: 180px;
    flex-shrink: 0;
  }

  .interest-desc {
    color: var(--color-text-secondary);
    font-size: var(--text-sm);
  }

  .publications-list {
    display: flex;
    flex-direction: column;
    gap: var(--space-md);
  }

  .publication-item {
    padding-bottom: var(--space-sm);
    border-bottom: 1px solid var(--color-border-light);
  }

  .publication-item:last-child {
    border-bottom: none;
  }

  .publication-title {
    font-weight: 500;
    color: var(--color-text);
    text-decoration: none;
    display: block;
    margin-bottom: var(--space-xs);
  }

  .publication-title:hover {
    color: var(--color-link);
  }

  .publication-venue {
    font-size: var(--text-sm);
    color: var(--color-text-secondary);
    font-style: italic;
    margin: 0;
  }

  @media (max-width: 640px) {
    .hero-intro {
      flex-direction: column;
      align-items: center;
      text-align: center;
    }

    .profile-photo {
      width: 100px;
      height: 100px;
    }

    .hero-links {
      justify-content: center;
    }

    .interest-item {
      flex-direction: column;
      gap: var(--space-xs);
    }

    .interest-area {
      min-width: auto;
    }
  }
</style>
